{
  "name": "ews-dados-pluviometricos",
  "version": "2.0.0",
  "description": "Dados pluviométricos automáticos — EWS GEE · CEMADEN + ANA + SNIRH + INMET",
  "private": true,
  "scripts": {
    "cemaden":  "node scripts/fetch-cemaden.js",
    "ana":      "node scripts/fetch-ana.js",
    "snirh":    "node scripts/fetch-snirh-wfs.js",
    "inmet":    "node scripts/fetch-inmet.js",
    "merge":    "node scripts/update-metadata.js",
    "build":    "npm run cemaden && npm run ana && npm run snirh && npm run inmet && npm run merge",
    "rapido":   "npm run cemaden && npm run inmet && npm run merge"
  },
  "dependencies": {
    "axios": "^1.7.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
